/**
 * Ultimate Tic-Tac-Toe SDK
 *
 * @module
 */

/**
 * Say, "hello!"
 *
 * @example
 * ```ts
 * console.log(greet("World"));
 * ```
 */
export function greet(name: string): string {
  return `Hello, ${name}!`;
}
